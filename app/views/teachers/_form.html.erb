<%= form_for @teacher, url: "/schools/#{current_school.id}/teachers" do |f| %>
  <% if teacher.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(teacher.errors.count, "error") %> prohibited this teacher from being saved:</h2>

      <ul>
        <% teacher.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <label for="invite_email">Email</label>
    <input type="email" name="invite[email]" id="invite_email">
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
