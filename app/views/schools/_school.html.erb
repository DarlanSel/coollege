<div class='col-4'>
  <%= link_to homepage_school_path(school.slug) do %>
    <div class="card">
      <h5 class="card-header"><%= school.name %></h5>
      <div class="card-body">
      </div>
    </div>
  <% end %>
</div>
